const message = "  Hello this is my first message  ";
const messageUpper=  message.toUpperCase()
message.trim() //just console it
message.replace("first", "Second") //just console it
console.log(message);
console.log(messageUpper);